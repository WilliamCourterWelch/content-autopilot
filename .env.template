# === Generated by setup.py — fill in any blanks ===

# Your podcast info
PODCAST_NAME=
PODCAST_NICHE=
TARGET_MARKET=
EPISODES_PER_DAY=3

# Content sources (set by setup wizard)
CONTENT_SOURCE_TYPES=
WEBSITE_URL=
WEBSITE_CRAWL_MODE=
WEBSITE_SECTION=
WEBSITE_SITEMAP_URL=
YOUTUBE_CHANNEL_URL=
RSS_FEED_URL=

# Topic discovery
REDDIT_SUBREDDITS=

# Fact checking
FACT_CHECK_RULES=

# NotebookLM (authenticated via setup wizard)
# storage_state.json is saved automatically

# Transistor.fm — podcast hosting
TRANSISTOR_API_KEY=
TRANSISTOR_SHOW_ID=

# Claude API — writes SEO titles, descriptions, blog posts
ANTHROPIC_API_KEY=
# Optional override if `.anthropic_model` is absent
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# GHL AI Studio lane (python3 run.py --ai-lane)
# Default formats: audio. Example: audio,report,infographic
AI_LANE_FORMATS=audio
# YouTube — file PATHS only. Never paste client ids or tokens into git.
# On the Grok box (runtime, @williamcourterwelch):
#   YOUTUBE_CLIENT_SECRETS=/home/box/.secrets/youtube-oauth-client.json
#   YOUTUBE_TOKEN=/home/box/.secrets/youtube-oauth-token.json
# Do not invent these. Leave blank until the files exist.
YOUTUBE_CLIENT_SECRETS=
YOUTUBE_TOKEN=
# unlisted (default, canary) or public
YOUTUBE_PRIVACY=unlisted
# Social (LinkedIn / X / FB). Leave blank until you have a real token.
SOCIAL_BUFFER_ACCESS_TOKEN=
SOCIAL_API_TOKEN=
# globalhighlevel.com — upgrade existing pillars only. Never thin new HTML.
# Comma-separated existing money-page URLs, or edit data/pillar-pages.json
GHL_PILLAR_PAGES=
GHL_SITE_DEDUPE_URL=
GHL_SITE_UPGRADE_WEBHOOK=

# Google Gemini — transcribes audio (optional, improves SEO)
GOOGLE_AI_API_KEY=
ENABLE_TRANSCRIPTION=false

# Email summaries (optional)
ENABLE_EMAIL=false
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=

# Blog site (optional). HARD: do not point this at globalhighlevel.com for thin generated HTML.
ENABLE_BLOG=false
SITE_URL=
SITE_NAME=
SITE_TAGLINE=
SITE_CATEGORIES=
AFFILIATE_LINK=
# Alias accepted by description CTA helper
GHL_AFFILIATE_LINK=
OG_IMAGE_URL=

# Blog design
SITE_THEME=dark
SITE_ACCENT_COLOR=#f59e0b
SITE_FONT=DM Sans
SITE_LOGO=

# Scheduler
RUN_MODE=manual
CYCLE_HOURS=25
